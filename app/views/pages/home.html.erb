<!-- <div align="center">
  <h2>
  <%= if current_user.role == 'guest' then "Please log in as Patient or Doctor" else  "Welcome back, " + current_user.first_name end %>
  </h2>
</div> -->

<% if current_user.role == 'guest' %>
    <div align="center">
        <h1>Welcome to</h1>
        <%= image_tag "logo.png", alt: "CBV X Stem", id: "main_icon" %>
        <h3>Please log in as a patient or doctor.</h3>
    </div>
<% else %>
    <div align="center">
        <%= image_tag "logo.png", alt: "CBV X Stem", id: "main_icon" %>
        <h3><%= "Welcome back, " + current_user.first_name %></h3>
    </div>
<% end %>
